<%= form_for(@post) do |f| %>
  <div class="field">
    <h2><%= f.label 'Get Ready To BoomIt!', :class => 'nuvo' %></h2><br />
    <%= f.text_area :status, :class => 'status-update' %>
  </div>
  <div class="actions">
    <%= f.submit "BoomIt", :class => 'nobutt nuvo rtxt' %>
  </div>
<% end %>
